export const convertToHtml = (data) => {
  const res = ``;
  return res;
};

export const convertToPlainText = (data) => {
  const res = `Order Ref : ${data.order_ref}\nDelivery date: ${data.delivery_date}\nComment: \t${data.comment}`;
  return res;
};
